<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parking.mapper.OwnerMapper">

    <!-- 插入业主记录，返回自增主键 -->
    <insert id="insert" parameterType="com.parking.model.Owner" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_owner (community_id, house_no, phone_number, id_card_last4,
                               real_name, status, account_status, create_time, update_time, is_deleted)
        VALUES (#{communityId}, #{houseNo}, #{phoneNumber}, #{idCardLast4},
                #{realName}, #{status}, #{accountStatus}, NOW(), NOW(), 0)
    </insert>

    <!-- 根据ID查询业主 -->
    <select id="selectById" resultType="com.parking.model.Owner">
        SELECT id, community_id, house_no, phone_number, id_card_last4, real_name,
               status, reject_reason, audit_admin_id, audit_time, account_status,
               create_time, update_time, is_deleted, deleted_time
        FROM sys_owner
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <!-- 根据手机号和小区ID查询业主 -->
    <select id="selectByPhoneAndCommunity" resultType="com.parking.model.Owner">
        SELECT id, community_id, house_no, phone_number, id_card_last4, real_name,
               status, reject_reason, audit_admin_id, audit_time, account_status,
               create_time, update_time, is_deleted, deleted_time
        FROM sys_owner
        WHERE phone_number = #{phoneNumber} AND community_id = #{communityId} AND is_deleted = 0
    </select>

</mapper>
