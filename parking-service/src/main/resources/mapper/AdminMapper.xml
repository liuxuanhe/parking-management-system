<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parking.mapper.AdminMapper">

    <!-- 插入管理员记录，返回自增主键 -->
    <insert id="insert" parameterType="com.parking.model.Admin" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_admin (community_id, username, password, real_name, phone_number,
                               role, status, login_fail_count, password_expire_time,
                               must_change_password, create_time, update_time, is_deleted)
        VALUES (#{communityId}, #{username}, #{password}, #{realName}, #{phoneNumber},
                #{role}, #{status}, #{loginFailCount}, #{passwordExpireTime},
                #{mustChangePassword}, NOW(), NOW(), 0)
    </insert>

    <!-- 根据用户名查询管理员 -->
    <select id="selectByUsername" resultType="com.parking.model.Admin">
        SELECT id, community_id, username, password, real_name, phone_number,
               role, status, login_fail_count, last_login_time, last_login_ip,
               password_expire_time, must_change_password,
               create_time, update_time, is_deleted, deleted_time
        FROM sys_admin
        WHERE username = #{username} AND is_deleted = 0
    </select>

    <!-- 根据ID查询管理员 -->
    <select id="selectById" resultType="com.parking.model.Admin">
        SELECT id, community_id, username, password, real_name, phone_number,
               role, status, login_fail_count, last_login_time, last_login_ip,
               password_expire_time, must_change_password,
               create_time, update_time, is_deleted, deleted_time
        FROM sys_admin
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <!-- 统计指定角色的管理员数量 -->
    <select id="countByRole" resultType="int">
        SELECT COUNT(*)
        FROM sys_admin
        WHERE role = #{role} AND is_deleted = 0
    </select>

    <!-- 更新管理员密码 -->
    <update id="updatePassword">
        UPDATE sys_admin
        SET password = #{password},
            must_change_password = #{mustChangePassword},
            update_time = NOW()
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <!-- 登录成功更新：重置失败次数，更新最后登录时间和IP -->
    <update id="updateLoginSuccess">
        UPDATE sys_admin
        SET login_fail_count = 0,
            last_login_time = NOW(),
            last_login_ip = #{lastLoginIp},
            update_time = NOW()
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <!-- 登录失败更新：增加失败次数 -->
    <update id="updateLoginFail">
        UPDATE sys_admin
        SET login_fail_count = login_fail_count + 1,
            update_time = NOW()
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <!-- 锁定账号 -->
    <update id="lockAccount">
        UPDATE sys_admin
        SET status = 'locked',
            update_time = NOW()
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <!-- 查询管理员列表，Super_Admin 传 null 查所有 -->
    <select id="selectList" resultType="com.parking.model.Admin">
        SELECT id, community_id, username, real_name, phone_number,
               role, status, login_fail_count, last_login_time, last_login_ip,
               must_change_password, create_time, update_time
        FROM sys_admin
        WHERE is_deleted = 0
        <if test="communityId != null">
            AND community_id = #{communityId}
        </if>
        ORDER BY create_time DESC
    </select>

    <!-- 解锁管理员账号 -->
    <update id="unlockAccount">
        UPDATE sys_admin
        SET status = 'active',
            login_fail_count = 0,
            update_time = NOW()
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <!-- 重置管理员密码 -->
    <update id="resetPassword">
        UPDATE sys_admin
        SET password = #{password},
            must_change_password = #{mustChangePassword},
            update_time = NOW()
        WHERE id = #{id} AND is_deleted = 0
    </update>

</mapper>
